

      <%= form_for(subsidiary) do |f| %>
      <% if subsidiary.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(subsidiary.errors.count, "error") %> prohibited this subsidiary from being saved:</h2>

        <ul>
          <% subsidiary.errors.full_messages.each do |message| %>
          <li><%= message %></li>
          <% end %>
        </ul>
      </div>
      <% end %>

      <div class="field">
        <%= f.label :name %>
        <%= f.text_field :name,class: 'form-control' %>
      </div>

      <div class="field">
        <%= f.label :address %>
        <%= f.text_field :address,class: 'form-control' %>
      </div>

      <div class="field">
        <%= f.label :phone %>
        <%= f.text_field :phone,class: 'form-control' %>
      </div>

      <div class="field">
        <%= f.label :photo %>
        <%= f.file_field :photo %>
      </div>
      <br>
      <div class="actions">
        <%= f.submit 'Actualizar sucursal',class: 'btn btn-primary' %>
      </div>
      <% end %>
      <br>
